import React from "react";
import SideBar from "components/side_bar";

const Store = () => {
    return(
        <React.Fragment>
            <SideBar />
            <h1>Store Section</h1>
        </React.Fragment>
    )
}
export default Store